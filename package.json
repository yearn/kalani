{
  "name": "kalani-monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "clean": "rm -R node_modules && rm -R packages/**/dist && rm -R packages/**/.next",
    "dev": "(cd packages/web && vite --port 3000)",
    "build": "(cd packages/web && tsc -b && vite build)"
  }
}
